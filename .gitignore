# Dependencies and builds (create-next-app extends this at scaffold time)
node_modules/
.next/
out/
coverage/

# Environment
.env
.env.*
!.env.example

# Local Supabase
supabase/.temp/

# Pipeline artifacts: screenshots from the walk driver and Playwright
.artifacts/
test-results/
playwright-report/
.lighthouseci/

# OS / editors
.DS_Store
Thumbs.db

# Mutation testing (Stryker)
.stryker-tmp/
reports/

# Added when the scaffold landed (F00). The env rules above still win:
# a bare `.env*` here would swallow `!.env.example`.
/.pnp
.pnp.*
/build
*.pem
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
.pnpm-debug.log*
.vercel

# Which checks `pnpm verify` skipped, and why. Written per run.
.verify-skips.json

# Stryker en modo inPlace deja sus archivos de arranque en la raíz cuando una corrida falla.
stryker-setup-*.js
